

    const arr = [1, 2, 3, 4, 5]
//     console.log(arr)

    // Traverse My Array
    let sum = 0 ;
    for (let i = 0; i < arr.length; i ++){
        // console.log(arr[i])

       // arr[i] = arr[i] + 2 // Increase Value Of Every Array

      //  sum += arr[i];
        
      //   if(arr[i] % 2 === 0) {
      //       console.log(arr[i]);
      //   }
    }
    console.log(sum);

